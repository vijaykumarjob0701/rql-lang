# Shared plan schemas (0.1.0-draft)

- `logical-plan.schema.json` — LogicalPlan IR
- `physical-plan.schema.json` — PhysicalPlan IR

Copies are also bundled under each language package (`schema_data/`) for installability.
These drafts are Hypothesis packaging, not a published standard.
